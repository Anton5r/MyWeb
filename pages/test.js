import Head from 'next/head'
import styles from '../styles/Home.module.css'
import React from "react";
import emailjs from 'emailjs-com';

export default function ContactUs() {

  function sendEmail(e) {
    e.preventDefault();    //This is important, i'm not sure why, but the email won't send without it

    emailjs.sendForm('service_71p2n6i', 'template_dexjqgl', e.target, 'XUi4bOe33oI4zGpmI')
      .then((result) => {
          window.location.reload()  //This is if you still want the page to reload (since e.preventDefault() cancelled that behavior) 
      }, (error) => {
          console.log(error.text);
      });
  }

  return (
    <>
      <Head>
        <title>Главная</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css"/>
      </Head>

      <h2 className={styles.text}>Педложение по обновление сайта</h2>
      <h3>Так как он ещё в разработке может не корректно работать</h3>

    <form className="contact-form" onSubmit={sendEmail}>
      <input type="hidden" name="contact_number" />
      <label className={styles.formin}>Имя</label>
      <input type="text" name="from_name" /><br/>
      <label className={styles.formin}>Почта</label>
      <input type="email" name="from_email" className={styles.formin_lab}/><br/>
      <label className={styles.formin}>Сообщение</label>
      <textarea name="message" className={styles.formin_lab}/><br/>
      <input type="submit" value="Отправить" className={styles.formin_lab}/>
    </form>
    </>
  )
} 